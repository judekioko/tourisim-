// Base URL of the deployed Django API (see the safaris-api repo).
// After deploying it (e.g. to Render), set this to that service's URL
// plus "/api", e.g. "https://safaris-api.onrender.com/api".
const API_BASE = "REPLACE_WITH_YOUR_API_BASE_URL/api";
